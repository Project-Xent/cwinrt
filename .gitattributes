# Normalize text to LF in the repo AND check out as LF on every platform.
# eol=lf overrides core.autocrlf, so a Windows runner can't produce a CRLF
# working tree -- which would drift the golden hashes (snapshot_golden compares
# committed files against freshly generated LF output) and break determinism.
* text=auto eol=lf
